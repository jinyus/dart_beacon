name: state_beacon
description: A reactive primitive for dart and flutter
version: 0.1.1
repository: https://github.com/jinyus/dart_beacon

environment:
    sdk: ^3.1.5

dev_dependencies:
    lints: ^3.0.0
    test: ^1.21.0

platforms:
    android:
    ios:
    linux:
    macos:
    web:
    windows:

topics:
    - state
    - signal
    - reactive
    - beacon
